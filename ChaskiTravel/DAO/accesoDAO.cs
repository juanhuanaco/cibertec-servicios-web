namespace ChaskiTravel.DAO
{
    public class accesoDAO
    {
        /* public List<Usuario> lstUsuario()
          {
              return new List<Usuario>{
               new Usuario{nombre="Juan",usuario="admi", pass="admi", Roles= new string[]{"Administrador"}},
               new Usuario{nombre="Felipa",usuario="cliente", pass="cliente", Roles= new string[]{"Cliente"}}

              };
          }

          public Usuario validacion(string usu, string pas)
          {
              return lstUsuario().Where(item => item.usuario == usu && item.pass == pas).FirstOrDefault();
          }*/
    }
}
